-Добавил класс IMT в который перенес метод IMT(прешлос переименовать в IMT1).
-Класс создал для того, чтобы метод был виден в тестах. 
-Тесты проверяют точечное поподание во все диапазоны и один тест на простой расчет.
-Метод изменил, тепер он возварщает строковое значение.
-Так же сдел запись в файл(запись идет в по дефолты в папку bin, файл назвывается IMT.txt). 
